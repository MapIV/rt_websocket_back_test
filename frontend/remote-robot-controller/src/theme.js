import { createTheme } from "@mui/material";

const theme = createTheme({
	palette: {
		type: "light",
		primary: {
			main: "#63e29a",
		},
		secondary: {
			main: "#f50057",
		},
	},
});

export default theme;
